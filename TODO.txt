Metadata enrichment:
https://docs.haystack.deepset.ai/docs/llmmetadataextractor
- extract metadata at index time? https://docs.haystack.deepset.ai/docs/metadata-filtering 
Then filtering:
https://docs.haystack.deepset.ai/docs/metadata-filtering

Retriever:
- use combined retriever https://docs.haystack.deepset.ai/docs/retrievers#advanced-retriever-techniques
- use multi-query retrieval? (MultiQueryTextRetriever, MultiQueryEmbeddingRetriever)
- do we want a semantic search pipeline or extractive QA pipeline?

-embedding dimension als constant
- top k als constant
- document chunks wieder auf ganze dokumente mergen
- schauen ob das pydantic metadata model richtig an Ollama übergeben wird
- "metadata" zu "structured data" oder so umbenennen

[HIER]
- logging verbessern
- integrate Surya OCR
- indexing metadata extractor:
    - umbenennen
    - fix temperature
- make GUI like Surya OCR
- show progress
- auto detect language feature (and then propagate to every other component)

- EVALUIERUNG
- constrain to extraction only (keine prediction von ICD codes)
- download die extrahierten infos
- wie kann die conversion quality evaluiert werden?

- export und import von pgdump
-> siehe chatgpt "Streamlit Postgres Dump"
-> custom paradedb container
-> muss noch pip3 installieren i think
-> docker compose anpassen, dass der das richtig buildet und so

- services in eigene container packen -> API-ify (uvicorn = FASTApi)
- config-ify um verschiedene setups zu ermöglichen (anderes embedding modell, anderes OCR, metadatenextractor welches modell gemma,llama, ...)
    - wie viele relevante docling setups gibt es (runterschreiben für Constantin) -> eine config für jedes relevante docling setups (nicht nur OCR)
    - die 4 intfloat modelle in configs packen (verschiedene größen), aber überlegen ob es noch andere relevante für deutsch gibt und auf dem laptop ausführbar sind < 0.6b parameter, über MTEB schauen
    - dasselbe für ollama extractor(<=3b) -> schauen welche modelle structured outputs können, aufschreiben welche ins setup passen aber structured output nicht können
-> zur evaluation und dann in ein paper
-> CS überlegt sich saubere Fragestellung nach der evaluiert wird